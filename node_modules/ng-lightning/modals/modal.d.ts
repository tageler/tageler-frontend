import { ElementRef, Renderer, EventEmitter } from '@angular/core';
import { NglModalFooter } from './footer';
import { NglModalHeaderTemplate } from './header';
export declare class NglModal {
    private renderer;
    header: string;
    size: 'large';
    directional: string | boolean;
    closeButton: ElementRef;
    headingId: string;
    open: any;
    openChange: EventEmitter<{}>;
    headerTpl: NglModalHeaderTemplate;
    footer: NglModalFooter;
    private _open;
    private _directional;
    constructor(renderer: Renderer);
    close(evt?: Event): void;
    stopPropagation(evt: Event): void;
    focusFirst(): void;
}
