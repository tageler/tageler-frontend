import { EventEmitter, OpaqueToken } from '@angular/core';
import { INglConfig } from './config.interface';
export declare const NGL_CONFIG: OpaqueToken;
export declare class NglConfig {
    _emitter: EventEmitter<{}>;
    private values;
    constructor(config?: INglConfig);
    update(config: INglConfig): void;
    get(key: string): any;
}
export declare function NglConfigurable(config?: {
    changeDetectorProperty: string;
}): (constructor: Function) => void;
