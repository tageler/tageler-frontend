import { ElementRef, Renderer, TemplateRef, OnChanges } from '@angular/core';
import { NglFormGroupAlternate } from './group-alt';
import { NglFormCheckbox } from '../elements/input';
import { NglFormLabelTemplate } from '../form-label';
export declare class NglFormGroupElement implements OnChanges {
    private groupAlt;
    private element;
    private renderer;
    contentEl: NglFormCheckbox;
    labelStr: string;
    labelTpl: NglFormLabelTemplate;
    _label: TemplateRef<any> | string;
    uid: string;
    readonly labelClass: string;
    constructor(groupAlt: NglFormGroupAlternate, element: ElementRef, renderer: Renderer);
    ngOnChanges(changes?: any): void;
    ngAfterContentInit(): void;
    private setFormLabel();
}
