import { Directive, ElementRef, Renderer, Optional } from '@angular/core';
import { NglPill } from './pill';
export var NglPillLink = (function () {
    function NglPillLink(pill, element, renderer) {
        if (!pill)
            return;
        renderer.setElementClass(element.nativeElement, 'slds-pill__label', true);
        pill.unlinked = false;
    }
    NglPillLink.decorators = [
        { type: Directive, args: [{
                    selector: 'a',
                },] },
    ];
    /** @nocollapse */
    NglPillLink.ctorParameters = function () { return [
        { type: NglPill, decorators: [{ type: Optional },] },
        { type: ElementRef, },
        { type: Renderer, },
    ]; };
    return NglPillLink;
}());
//# sourceMappingURL=pill-link.js.map